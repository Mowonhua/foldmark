## 主题边界

新增或修改主题时，先阅读 [主题格式](docs/主题.md)。主题包负责配色、阴影、圆角及交互状态的视觉参数；`src/app.css` 和组件样式只定义布局、交互规则并消费通用语义变量，不得按主题名称、ID 或具体风格写条件分支，也不得将这类分支搬到其他应用代码中。扩展视觉能力时同步更新通用参数契约、校验与文档，并验证缺省回退、主题及明暗切换无残留、导入导出和持久化保留参数。
